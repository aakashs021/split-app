import 'package:flutter/material.dart';

class DetailPaymentDone extends StatelessWidget {
  const DetailPaymentDone({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Detail Payment done'),),
      body: Scaffold(),
    );
  }
}