class ExpenseModel {
  String description;
  int paid;
  num amount;
  DateTime dateTime;
  ExpenseModel({
    required this.description,
    required this.paid,
    required this.amount,
    required this.dateTime,
  });
  
}
